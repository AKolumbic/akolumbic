# Try the most current resolution mode first (most reliable)
resolution-mode=highest

# Fall back to legacy peer deps only if needed
legacy-peer-deps=false

# Set registry
registry=https://registry.npmjs.org/

# Make sure we're getting production dependencies properly
production=true 