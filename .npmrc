# Set resolution-mode to highest for best dependency resolution
resolution-mode=highest

# Enable legacy-peer-deps for compatibility
legacy-peer-deps=true

# Set registry
registry=https://registry.npmjs.org/

# We need dev dependencies for build
production=false 